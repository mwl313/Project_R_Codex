# 🔮 Project R — 초능력 알까기

> **Design Document v1.0** — _2026-07-09_
> 
> 본 문서는 지속적으로 업데이트되는 살아있는 설계 문서다.
> 코드·밸런스·컨셉 변경 시 이 문서도 함께 갱신한다.

---

## 1. 게임 개요

### 1.1 정체성

| 항목 | 값 |
|---|---|
| 장르 | 턴제 물리 전략 대전 (1v1) |
| 플랫폼 | Web 우선 (LÖVE2D → LOVE.js), 추후 Steam / 모바일 |
| 타겟 | 캐주얼~미드코어 PvP 플레이어 |
| 판당 시간 | 3~5분 |
| 플레이어 수 | 2인 |

### 1.2 코어 판타지

> **"내 초능력으로 물리 법칙을 비틀어 상대를 압도한다"**

초능력 알까기는 모든 플레이어가 **같은 기본기(알 배치 + 드래그 앤 슛)** 를 사용하지만,
각자 선택한 캐릭터의 **고유한 초능력** 하나로 전장을 뒤흔드는 비대칭 PvP 게임이다.

### 1.3 승리 조건

- **주 승리:** 상대방의 모든 알을 보드 밖으로 아웃
- **판정승 (미구현):** 10턴 경과 시 생존 알이 많은 쪽 승리 (무승부 방지)

---

## 2. 게임 플로우

```
┌──────────┐    ┌──────────┐    ┌──────────────────────┐    ┌──────────┐
│ 캐릭터   │ → │  알 배치  │ → │     턴 루프 ×N       │ → │  승/패   │
│ 선택     │    │  (동시)   │    │                      │    │  결과    │
└──────────┘    └──────────┘    └──────────┬───────────┘    └──────────┘
                                           │
                          ┌────────────────┼────────────────┐
                          ▼                ▼                ▼
                    ┌──────────┐    ┌──────────┐    ┌──────────┐
                    │ 알 선택   │ → │ 조준·샷   │ → │ 물리 시뮬 │
                    │ (클릭)    │    │ (드래그)  │    │ 결과 확인  │
                    └──────────┘    └──────────┘    └─────┬────┘
                                                          │
                                    ┌─────────────────────┘
                                    ▼
                             ┌────────────┐
                             │ 초능력     │
                             │ 발동 결정  │ (충전 ≥100% 시)
                             └────────────┘
```

### 2.1 페이즈 상세

| 순서 | 페이즈 | 설명 | 기존 코드 |
|---|---|---|---|
| 1 | `CHARACTER_SELECT` | 4종 캐릭터 중 선택 | **신규** |
| 2 | `PLACEMENT_PRIVATE` | 3-2-2 포메이션, 동시 배치 | `match_scene.lua` 재활용 |
| 3 | `PLACEMENT_REVEAL` | 상호 배치 공개 (3초) | 그대로 |
| ~~4~~ | ~~`CARD_SELECT`~~ | **제거** — 초능력 충전 시스템으로 대체 | 코드 제거 |
| 4 | `PLAYING` | 턴제 샷 + 초능력 | `match_scene.lua` 수정 |
| 5 | `RESULT` | 승패 + 리매치 | 그대로 |

### 2.2 턴 구성

1. **알 선택:** 자신의 살아있는 알 중 하나를 클릭
   - 잠금 상태(초능력 효과, 바인드 등)인 알은 선택 불가
2. **조준 & 샷:** 드래그로 방향과 파워 결정, 릴리즈로 발사
   - 파워 = 드래그 거리 × `POWER_PER_PIXEL` (최대 `MAX_SHOT_POWER`)
   - 예측 궤적 점선 + 반사 미리보기 표시
3. **물리 시뮬레이션:** 충돌 → 반사 → 감쇠 → 아웃 판정
4. **초능력 발동 결정:** 충전 게이지 100% 시 발동 가능
   - 샷 전에 발동하거나, 샷 후에 발동하거나 — 턴 내 자유
   - 발동 후 충전 게이지 0% 리셋

### 2.3 턴 종료 조건

- 샷이 1회 발사되고 모든 물리 시뮬레이션이 종료되면 상대 턴으로 전환
- 턴 제한 시간 20초 초과 시 자동 턴 전환

---

## 3. 캐릭터 & 초능력

### 3.1 충전 시스템 (공통)

| 파라미터 | 값 | 설명 |
|---|---|---|
| `CHARGE_PER_TURN` | 25% | 매 턴 시작 시 자동 충전 |
| `CHARGE_ON_ALLY_OUT` | 15% | 내 알 1개 아웃될 때마다 추가 충전 (역전 기회) |
| `CHARGE_MAX` | 100% | 이 도달 시 초능력 발동 가능 |
| `CHARGE_POST_USE` | 0% | 초능력 사용 후 리셋 |

> **평균 발동 주기:** 4턴에 1회 (역전 상황 시 더 빠름)

### 3.2 캐릭터 목록

#### 🗡️ 나이트로드 (Night Lord)
| 항목 | 값 |
|---|---|
| 초능력 | **섀도우스텝** (Shadow Step) |
| 효과 | 내 알 1개를 보드 내 **원하는 위치로 순간이동**. 이동 후 추가 샷 가능. |
| 대상 지정 | 포인트 클릭 (알 선택 → 목적지 선택) |
| 전략 | 기습, 포지셔닝 재편, 예측 불가한 각도 창출 |
| 판타지 | "그림자처럼 나타나 빈틈을 찌른다" |
| 구현 참조 | `abilities.lua` `reinforcement` 배치 로직 이식 + 순간이동 |

#### 🔥 아크메이지 (Arch Mage)
| 항목 | 값 |
|---|---|
| 초능력 | **메테오** (Meteor) |
| 효과 | 보드 중앙에 **운석 낙하**. 주변 모든 알에 `SHOCKWAVE_STRENGTH` ×1.5 충격파. 낙하 지점에 장애물 생성. |
| 대상 지정 | 논타겟 (자동 중앙) |
| 전략 | 광역 제압, 맵 컨트롤, 구석 농성 견제 |
| 판타지 | "하늘에서 운석을 떨궈 전장을 초토화한다" |
| 구현 참조 | `abilities.lua` `shockwave` + `rockfall` 조합 |

#### 🛡️ 팔라딘 (Paladin)
| 항목 | 값 |
|---|---|
| 초능력 | **디바인실드** (Divine Shield) |
| 효과 | 이번 턴 + 다음 상대 턴까지 **내 모든 알 무적** (밀려나지 않음) + 충돌 시 상대 알에 반사 데미지. |
| 대상 지정 | 논타겟 (즉시 발동) |
| 전략 | 방어적 플레이, 카운터, 상대가 몰아칠 때 역이용 |
| 판타지 | "철벽 방어로 모든 공격을 튕겨낸다" |
| 구현 참조 | `abilities.lua` `invincible` (턴 오프셋 2로 확장 + 반사 효과 추가) |

#### ⚔️ 아란 (Aran)
| 항목 | 값 |
|---|---|
| 초능력 | **콤보피니셔** (Combo Finisher) |
| 효과 | 이번 턴 **연속 2샷** 가능. 첫 샷으로 적 알이 1개 이상 아웃되면 두 번째 샷의 파워가 **1.5배** 증가. |
| 대상 지정 | 패시브 (턴 시작 시 발동 선언) |
| 전략 | 공격형, 연속기로 몰아치기, 리스크 감수 |
| 판타지 | "한 번 잡으면 콤보로 끝까지 몰아붙인다" |
| 구현 참조 | `abilities.lua` `agile` (shot_budget 확장) + 조건부 파워 배율 |

### 3.3 밸런스 매트릭스

| 캐릭터 | 난이도 | 공격 | 방어 | 컨트롤 | 기동성 |
|---|---|---|---|---|---|
| 나이트로드 | ★★★☆ | ★★☆ | ★★☆ | ★★★ | ★★★★ |
| 아크메이지 | ★★★ | ★★★★ | ★★ | ★★★★ | ★★ |
| 팔라딘 | ★★☆ | ★★ | ★★★★★ | ★★ | ★★ |
| 아란 | ★★★★ | ★★★★★ | ★ | ★★ | ★★☆ |

---

## 4. 물리 시스템

### 4.1 엔진 스펙

| 파라미터 | 값 | 파일 |
|---|---|---|
| `BOARD_W` / `BOARD_H` | 600 / 600 | `shared/gameplay_rules.json` |
| `STONE_RADIUS` | 14 | ↑ |
| `STONE_COUNT_PER_PLAYER` | 7 | ↑ |
| `STONE_MASS` | 1.0 | ↑ |
| `PHYSICS_DAMPING_PER_SEC` | 1.8 | ↑ |
| `PHYSICS_RESTITUTION` | 0.86 | ↑ |
| `PHYSICS_STOP_SPEED` | 12 | ↑ |
| `PHYSICS_FIXED_STEP_SEC` | 0.016 | ↑ |
| `PHYSICS_MAX_SIM_SEC` | 6.0 | ↑ |
| `MAX_SHOT_POWER` | 1500 | ↑ |
| `POWER_PER_PIXEL` | 4.0 | ↑ |
| `SHOT_SPEED_SCALE` | 0.60 | ↑ |

### 4.2 물리 규칙

- 모든 충돌은 `physics_engine.lua`에서 처리 (멀티/싱글 공용)
- 알-알 충돌: 반발 계수(`PHYSICS_RESTITUTION`) + 질량 기반 운동량 보존
- 알-장애물 충돌: AABB + 원 충돌 감지, 반사
- 보드 경계: 벽 반사 (튕김)
- 정지 판정: 속도 < `PHYSICS_STOP_SPEED` 이면 강제 정지
- 최대 시뮬레이션: 6초 초과 시 강제 종료

### 4.3 샷 파워 공식

```
power = min(MAX_SHOT_POWER, drag_distance × POWER_PER_PIXEL)
speed = power × SHOT_SPEED_SCALE
vx = direction_x / |direction| × speed
vy = direction_y / |direction| × speed
```

---

## 5. 아키텍처

### 5.1 기술 스택

| 계층 | 기술 | 역할 |
|---|---|---|
| 클라이언트 | LÖVE2D 11.x + Lua | 게임 렌더링, 입력, 로컬 물리 |
| 서버 | Cloudflare Workers + Durable Objects (TypeScript) | 방 매칭, 권위적 게임 상태, 채팅 |
| 데이터 | JSON SSOT (`shared/*.json`) | 게임룰·카드·밸런스 상수 |
| 통신 | HTTP Long-Poll | 클라이언트-서버 상태 동기화 |

### 5.2 디렉토리 구조 (변경 예정 포함)

```
ProjectR/
├── main.lua                  # 진입점 (변경 없음)
├── conf.lua                  # LÖVE2D 설정
├── constants.lua             # 게임 상수 (shared/*.json → 로드)
├── config.lua                # 런타임 플래그
├── game_mechanics.lua        # 매치 물리 추상화 계층
├── physics_engine.lua        # 순수 물리 시뮬레이션
├── abilities.lua             # 카드/초능력 효과 적용기
├── app.lua                   # 앱 코어
├── managers/                 # 씬·사운드·트랜지션·렌더스케일·설정
├── ui/                       # UI 위젯
│   ├── charge_gauge.lua      # [신규] 충전 게이지 위젯
│   ├── aim_preview.lua       # [신규] 조준 예측선 위젯
│   ├── card_hand_bar.lua     # [제거 예정] → 초능력 버튼으로 대체
│   └── ...
├── scenes/
│   ├── character_select_scene.lua  # [신규] 캐릭터 선택
│   ├── match_scene.lua             # [대수정] 카드 페이즈 제거, 초능력 통합
│   ├── lobby_scene.lua             # (변경 없음)
│   ├── ...
│   └── single_*.lua                # [보존] 싱글플레이는 추후 재활용
├── shared/                   # JSON SSOT (Single Source of Truth)
│   ├── gameplay_rules.json   # 물리·룰 상수
│   ├── characters.json       # [신규] 캐릭터·초능력 정의
│   ├── card_rules.json       # [축소] 멀티 관련만 유지
│   └── ...
├── server/                   # Cloudflare Workers
│   └── src/
│       ├── index.ts          # API 라우터
│       ├── room_do.ts        # [수정] 카드 페이즈 제거, 초능력 액션 추가
│       ├── abilities.ts      # [수정] 초능력 효과 서버 검증
│       └── ...
├── net/                      # 클라이언트 통신 (HTTP)
├── effects/                  # 이펙트 매니저
├── i18n/                     # 다국어 (ko/en)
├── assets/                   # 폰트·사운드·이미지
├── DESIGN_DOC.md             # [이 문서]
└── MEMORY.md                 # 프로젝트 메모리 (개발 중 결정사항)
```

---

## 6. 변경 작업 (코드 기준)

### 6.1 제거 대상

| 대상 | 이유 |
|---|---|
| `PHASE_CARD_SELECT` | 초능력 충전 시스템으로 대체 |
| `card_hand_bar.lua` (하단 카드 핸드) | 초능력 버튼 1개로 대체 |
| 싱글 전용 카드 (`guard_shell`, `quick_reload`, `stone_polish`, `trick_bounce`, `legend_overdrive`, `single_example_card`) | 멀티플레이에서는 미사용 |
| `reward_picker.lua`, `relic_effects.lua` 등 싱글플레이 보상 | 싱글 콘텐츠는 추후 재활용 |
| `single_*.lua` (전체) | 추후 싱글플레이 완성 시 재활용, 현재는 미사용 |
| `PHASE_CARD_SELECT` 관련 `room_do.ts` 로직 | 서버에서도 카드 선택 제거 |

### 6.2 수정 대상

| 대상 | 변경 내용 |
|---|---|
| `match_scene.lua` | 카드 선택 페이즈 제거. 초능력 버튼·충전 게이지 UI 추가. 충전 로직 통합. |
| `abilities.lua` | 5종 카드 능력 → 4종 초능력 + 하위 효과 함수 리팩터. 초능력 발동 함수 추가. |
| `shared/card_rules.json` | 카드 정의 축소 (멀티 전용만). 초능력 관련 데이터 이전. |
| `room_do.ts` (서버) | 카드 선택 상태 제거. 초능력 액션(`use_ability`) 메시지 처리 추가. 충전 상태 검증. |
| `server/src/abilities.ts` | 서버 측 초능력 효과 검증 로직 추가. |
| `constants.lua` | `PHASE_CARD_SELECT` 제거. `PHASE_CHARACTER_SELECT` 추가. |
| `app.lua` | 씬 라우팅에 캐릭터 선택 씬 추가. |
| `game_mechanics.lua` | 충전 상태 관리 콜백 추가. |

### 6.3 신규 제작 대상

| 대상 | 설명 |
|---|---|
| `characters.json` | 캐릭터 4종 + 초능력 정의 (SSOT) |
| `character_select_scene.lua` | 4종 캐릭터 선택 UI |
| `charge_gauge.lua` | 충전 게이지 + 초능력 버튼 위젯 |
| `aim_preview.lua` | 조준 예측선 드로잉 (반사 미리보기 포함) |
| 서버: 초능력 액션 메시지 | `ability_action` → 서버 검증 → 브로드캐스트 |

---

## 7. 네트워크 설계

### 7.1 주요 메시지 (변경)

| 메시지 타입 | 방향 | 설명 |
|---|---|---|
| `character_select` | C→S | 캐릭터 선택 |
| `place_stones` | C→S | 알 배치 제출 |
| `shot_action` | C→S | 샷 발사 (stoneId, dirX, dirY, power) |
| `ability_action` | C→S | **초능력 발동** (abilityId, target?) |
| `charge_sync` | S→C | 충전 게이지 상태 동기화 |
| `snapshot` | S→C | 전체 게임 상태 델타 |
| `turn_change` | S→C | 턴 전환 알림 |

### 7.2 권위 (Authority)

- **Server-authoritative:** 충전 게이지, 초능력 발동 가능 여부, 아웃 판정, 턴 관리
- **Client-predictive:** 샷 발사 (기존 predictive shot 유지, 서버 거절 시 롤백)

---

## 8. UI/UX 설계

### 8.1 인게임 레이아웃 (1280×720 기준)

```
┌─────────────────────────────────────────────────────┐
│  상대 닉네임       [충전 ●●●○○ 60%]       타이머     │ ← 상단 바
│                  [팔라딘 초상]                        │
├─────────────────────────────────────────────────────┤
│                                                     │
│                 ┌───────────────┐                    │
│                 │               │                    │
│                 │   보드 600×600 │                    │
│                 │               │                    │
│                 │   ●  ●  ●    │ ← 상대 알 (주황)   │
│                 │     ●  ●     │                    │
│                 │     ●  ●     │                    │
│                 │   ●  ●  ●    │ ← 내 알 (파랑)     │
│                 │               │                    │
│                 └───────────────┘                    │
│                                                     │
├─────────────────────────────────────────────────────┤
│  [나이트로드]  충전 ●●●●● 100%  [⚡ 섀도우스텝]     │ ← 하단 초능력 바
└─────────────────────────────────────────────────────┘
```

### 8.2 조준 UI

- 드래그 중: 알에서 마우스까지 실선 + 반대 방향 점선 (예측 궤적)
- 보드 경계에서의 예상 반사점 표시 (작은 원)
- 파워 인디케이터: 알 주변에 숫자 + 색상 그라데이션
- 릴리즈: 샷 발사 → 궤적 트레일 + 충돌 이펙트

### 8.3 충전 게이지 상세

- 하단에 캐릭터 초상화 + 원형 게이지
- 100% 도달 시 게이지 테두리가 빛나는 애니메이션
- 초능력 버튼이 활성화되고 펄스 효과
- 사용 직후 0%로 떨어지는 애니메이션 (0.5초)

---

## 9. 비주얼 & 사운드

### 9.1 MVP (Phase 1) 필수 에셋

| 에셋 | 타입 | 용도 |
|---|---|---|
| 캐릭터 초상 4종 | PNG 128×128 | 선택 화면 + 인게임 초상 |
| 알 스킨 8종 | PNG 64×64 | 캐릭터별 알 (4종 × 내/상대용) |
| 보드 배경 | PNG 600×600 | 기본 보드 텍스처 |
| 샷 트레일 | 파티클 | 샷 발사 시 궤적 |
| 충돌 이펙트 | 파티클 | 알-알, 알-벽 충돌 |
| 초능력 이펙트 | 파티클 + 스크린 | 캐릭터별 초능력 발동 연출 |

### 9.2 사운드 (Phase 2)

| 사운드 | 설명 |
|---|---|
| `shot_charge` | 드래그 시작 → 파워 증가 루프 |
| `shot_release` | 샷 발사 |
| `collision_stone` | 알-알 충돌 |
| `collision_wall` | 벽 충돌 |
| `stone_out` | 알 아웃 |
| `ability_activate` | 초능력 발동 (캐릭터별 상이) |
| `charge_full` | 충전 100% 도달 |
| `turn_change` | 턴 전환 |
| `victory` / `defeat` | 승리 / 패배 |

---

## 10. 개발 로드맵

### Phase 1 — 알파 (현재 ~ 2주)

**목표:** 친구랑 1v1 가능한 상태

```
□ 초능력 충전 시스템 코어 구현
□ 캐릭터 4종 + 초능력 4종 구현
□ 카드 선택 페이즈 제거
□ 초능력 발동 페이즈로 교체
□ 캐릭터 선택 씬
□ 서버: ability_action 메시지 처리
□ 조준선 시각화 개선 (예측 궤적)
□ 충전 게이지 UI
```

### Phase 2 — 폴리시 (2~4주)

**목표:** itch.io 데모 출시 가능

```
□ 초능력 발동 컷신/이펙트
□ 충돌 이펙트 + 트레일
□ 캐릭터 초상·알 스킨 (플레이스홀더도 OK)
□ 사운드 SFX (최소 세트)
□ 튜토리얼 씬
□ 밸런스 1차 튜닝 (유저 피드백 기반)
□ 승패 기록
□ LOVE.js 웹 빌드 파이프라인
```

### Phase 3 — 런칭 (4~8주)

```
□ itch.io 공개 데모
□ 밸런스 2차 튜닝
□ 랭크 / 연승 표시
□ 친구 초대 링크
□ 관전 모드
□ Steam 페이지 검토
```

### Phase 4 — Beyond (미정)

```
□ 싱글플레이 캠페인 완성 (기존 코드 재활용)
□ 모바일 포팅
□ 신규 캐릭터 추가
□ 시즌제 콘텐츠
```

---

## 11. 밸런스 노트

### 11.1 디자인 원칙

- **방어도 역전 가능해야 한다.** 팔라딘이 너무 강하면 게임이 느려진다.
- **초능력 없이도 이길 수 있어야 한다.** 기본기만으로 승리 가능한 밸런스 하한선.
- **모든 매치업이 40~60% 승률 안에 들어야 한다.** 7:3 이상 차이나면 긴급 패치.

### 11.2 주시할 밸런스 포인트

| 포인트 | 현재값 | 리스크 |
|---|---|---|
| 충전 속도 (25%/턴) | 4턴에 1회 | 너무 빠르면 초능력 난사, 너무 느리면 기본기만 반복 |
| 아웃 시 충전 (15%) | 최대 +45%/턴 | 역전이 너무 쉬우면 전략성 저하 |
| 팔라딘 무적 턴 (2턴) | 자신 + 다음 상대 | 2턴 무적이 너무 긴지 확인 필요 |
| 아란 보너스 파워 (×1.5) | 조건부 | 조건 달성이 너무 어려우면 하위 티어 |
| 메테오 충격파 (×1.5) | 일반 충격파 대비 | 중앙 타격이 너무 강하면 구석 전략 필수화 |

### 11.3 튜닝 파라미터 (SSOT)

모든 밸런스 수치는 `shared/characters.json`에서 관리:

```json
{
  "characters": {
    "night_lord": {
      "ability": {
        "charge_rate": 0.25,
        "gain_on_ally_out": 0.15
      }
    }
    // ...
  },
  "global": {
    "charge_per_turn": 0.25,
    "charge_on_ally_out": 0.15,
    "charge_max": 1.0,
    "turn_time_limit_sec": 20
  }
}
```

---

## 12. 체인지로그

| 날짜 | 버전 | 변경 내용 |
|---|---|---|
| 2026-07-09 | v1.0 | 초안 작성. 기존 코드 분석 기반 멀티플레이어 초능력 알까기 디자인. |

---

> **편집 규칙:**
> - 본 문서는 `projects/ProjectR/DESIGN_DOC.md`에 위치한다.
> - 밸런스·룰·아키텍처 변경 시 해당 섹션을 업데이트하고 체인지로그에 기록한다.
> - Phase 완료 시 로드맵 섹션의 체크박스를 갱신한다.
> - 디자인 결정은 `@role` / `@audit` 프레임워크 도구로 검증 후 반영한다.
